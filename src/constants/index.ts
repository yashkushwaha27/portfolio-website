const particlesConfig = require('./particlesConfig.json');
const particlesConfig2 = require('./particlesConfig2.json');
import { zIndex } from '../theme/zIndex';
import { content } from './content';
import { keyCodes } from './keyCodes';

export { particlesConfig, particlesConfig2, zIndex, content, keyCodes };
